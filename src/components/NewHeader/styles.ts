import styled from "styled-components";

export const HeaderContainer = styled.div`
  width: 100%;
  height: 6.6rem;
  background: #fff;
  box-shadow: 0px 2px 10px 0px rgba(0, 0, 0, 0.15);
`;

export const TextIcon = styled.div`
  display: flex;
  flex-direction: row;
  align-items: center;
  gap: 0.8rem;

  padding: 2.2rem 0 2.2rem 10.8rem;

  h1 {
    font-family: 'Noto Sans', sans-serif;
    font-size: 1.8rem;
    font-style: normal;
    font-weight: 700;
    line-height: 2.2rem;
    color: #003E43;
  }
`;